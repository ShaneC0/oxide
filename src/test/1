init 
    int length, width, area;
    length = 25;
    width = 50;
    area = length * width;
    bool flag;
    if(flag && true || false) then
        flag = false;
    endif;
    print(area);
halt
